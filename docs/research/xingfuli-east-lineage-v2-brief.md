# Xingfuli East Lineage v2 Brief

## Scope

- Asset: `xingfuli-east` only.
- Frozen read-only parent: accepted Hero Blend and GLB.
- New paths only: `identity-v2` and `massing-v2`; current three binaries, public resolver, registry and status remain untouched.
- Trees, decoration, full-map content and Recovery/Hold are excluded.

## Evidence boundary

- Reused evidence: `docs/research/xingfuli-reference-manifest.json`, `docs/research/xingfuli-model-brief.md`, and existing east map audit.
- User's nine first-party photographs are recorded as pending raw access; they are not claimed as reviewed here. Whether photo 9 reaches Panyu Road is unknown.
- The known Panyu Road overlap is not altered: local OSM proves only the site axis, not an east-only footprint.

## Candidate derivation

- Hero -> Identity v2: retain the inherited lane base and Panyu-entry matrix hardscape so the tier bounds and ground/entry semantics remain stable; remove Hero-only material details while retaining the white glass corner, roof tank, long south timber facade, brick window volume, storefront rhythm and overall silhouette.
- Identity v2 -> Massing v2: remove sills and extra window columns only. Keep the entrance volumes and long-axis silhouette unchanged.
- The generator verifies parent GLB and Blend SHA before opening the parent and exports only new v2 paths.

## Fixed views and gates

- Canonical and side cameras are the existing deterministic headless views.
- Side view: local `(0.0, -20.0, 8.0)` toward `(37.0, -6.5, 4.0)`; it stays outside the lane to preserve two east volumes and their depth without a near facade occluder.
- Street view: local `(53.0, -18.0, 10.0)` toward `(34.0, -6.0, 4.0)`, an elevated Panyu-facing oblique view of the white glass corner, matrix entry and long south facade. Both are visual review only and do not assert road clearance.
- Before promotion: main-window MCP 1/2/3, map/road decision, and Three.js tier/fallback/performance/collision QA are still required.
